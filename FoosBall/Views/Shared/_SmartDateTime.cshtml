@model DateTime
@{
    //var matchTime = Model.TimeOfDay;
    var matchTime = new DateTime(2013, 4, 3).TimeOfDay;
    var now = DateTime.Now.TimeOfDay;
    var timespan = now.Subtract(matchTime);
}
